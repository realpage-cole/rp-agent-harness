node_modules/
node_modules
out/
dist/
.DS_Store
*.log
.vite/

# Runtime hive home — coordination data the app writes when the harness home
# resolves to the repo root (agents/, teams/, board, tasks, logs). Never source.
/hive/
/teams/

.env
.env.local
.env.signing
.gstack/

# signing / notarization secrets — NEVER commit these
*.p8
*.p12
*.cer
*.mobileprovision
build/AuthKey_*.p8
